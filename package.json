{
  "name": "camera-file-bin",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start:sender": "parcel serve sender/index.html --port 8081 --host 0.0.0.0 --dist-dir dist/sender --open",
    "start:receiver": "parcel serve receiver/index.html --port 8082 --dist-dir dist/receiver --open",
    "build": "parcel build sender/index.html receiver/index.html --dist-dir build"
  },
  "dependencies": {
    "bwip-js": "^2.0.10",
    "@zxing/library": "^0.20.0"
  },
  "devDependencies": {
    "parcel": "^2.10.0"
  }
}
